<div class="user_request">

   
        <div>
            <%= User.find(friend_request.user_id).firstname %>
            <%= User.find(friend_request.user_id).lastname %>
        </div>
   
    
   
    <div class="accept_button">
        <%= form_with(url: friend_requests_path, method: "post") do %>
            <input type="hidden" name="friend_request[request_id]" value="<%= friend_request.id %>" autocomplete="off">
            <%= submit_tag("Accept", class: "btn") %>
        <% end %>
    </div>
    
</div>
